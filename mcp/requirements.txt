# XConnect MCP bridge (xconnect_mcp.py) — pinned runtime dependencies.
#   python3 -m venv venv && venv/bin/pip install -r requirements.txt
# Direct imports are pinned here; pip resolves their transitive deps (anyio,
# starlette, uvicorn, etc.). A full lockfile/SBOM is generated in CI (see S1).
httpx==0.28.1
mcp==1.28.0
pydantic==2.13.4
